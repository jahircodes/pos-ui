
  # Mobile POS System UI

  This is a code bundle for Mobile POS System UI. The original project is available at https://www.figma.com/design/yknKKhRXO9hE5ve3fAAZYq/Mobile-POS-System-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  